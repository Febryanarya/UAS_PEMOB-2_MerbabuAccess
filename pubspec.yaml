name: merbabuaccess_app
description: A Merbabu hiking booking application.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.2.0
  fluttertoast: ^8.2.4


  # Utils
  cupertino_icons: ^1.0.8
  provider: ^6.0.5
  http: ^1.2.1
  intl: ^0.19.0

  shared_preferences: ^2.2.2  # ✅ Tambah ini
  qr_flutter: ^4.1.0          # ✅ Untuk QR Code (nanti)
  image_picker: ^1.0.4


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true